# Зависимости (папка с библиотеками npm)
node_modules/

# Переменные окружения (секретные ключи, пароли, API-ключи)
.env

# База данных SQLite (чтобы локальная база не перезаписывала базу на сервере)
*.db
*.db-journal
*.db-shm
*.db-wal

# Логи
npm-debug.log*
yarn-debug.log*
yarn-error.log*
logs/
*.log

# Папки сборки и кэш (если появятся в будущем)
dist/
build/
.cache/

# Системные файлы
.DS_Store
Thumbs.db